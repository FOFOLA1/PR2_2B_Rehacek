<UserControl x:Class="Sudoku.Num"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sudoku"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="50"
             Background="AliceBlue">

    <Border BorderThickness="1.1" BorderBrush="Black" Background="AliceBlue" x:Name="border">

        <Grid>
            <Label Content="{Binding CommentsString, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="7pt"
                   Foreground="SlateGray"
                   HorizontalContentAlignment="Left"
                   VerticalContentAlignment="Top"
                   HorizontalAlignment="Stretch"
                   VerticalAlignment="Stretch"
                   Padding="5,0"
                   Margin="0"
                   />

            <Label Content="{Binding Symbol, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="30pt"
                   HorizontalContentAlignment="Center"
                   VerticalContentAlignment="Center"
                   HorizontalAlignment="Stretch"
                   VerticalAlignment="Stretch"
                   Cursor="Hand"
                   Padding="0"
                   Margin="0"
                   />
        </Grid>
        
        

        <!--
        <TextBlock Text="{Binding Symbol, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="30pt"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Cursor="Hand"
                   Width="50"
                   Height="50"
                   />
        -->

    </Border>
</UserControl>
